<UserControl x:Class="AutoPlannerHelpers.Views.CTExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoPlannerHelpers.Views"
             xmlns:Behaviors="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
             mc:Ignorable="d">
    <Grid>
        <Border Margin="0" BorderThickness="1,0,0,0" BorderBrush="Black">
            <StackPanel>
                <Grid>
                    <Label Content="Specify CT Image to Export" FontSize="20" FontStyle="Normal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0"/>
                    <Button Content="?" HorizontalAlignment="Right" Width="24" Height="20" Margin="0,0,10,0" Command="{Binding ShowExportCTInfoCommand}"></Button>
                </Grid>
                <Grid Margin="25,5,0,0">
                    <Label Content="Series ID" Margin="15,0,0,0"></Label>
                    <Label Content="CT ID" Margin="165,0,0,0"></Label>
                    <Label Content="No of images" Margin="270,0,0,0"></Label>
                    <Label Content="Creation date" Margin="365,0,0,0"></Label>
                    <Label Content="Select?" Margin="470,0,0,0"></Label>
                </Grid>
                <ScrollViewer Height="334" Width="585" Margin="0,5,0,0" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <ItemsControl x:Name="ExportCTSP" ItemsSource="{Binding CTImageList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding SeriesId}" HorizontalContentAlignment="Center" IsReadOnly="True" Margin="5"/>
                                    <TextBox Grid.Column="1" Text="{Binding CTId}" HorizontalContentAlignment="Center" IsReadOnly="True" Margin="5"/>
                                    <TextBox Grid.Column="2" Text="{Binding NumberOfSlices}" HorizontalContentAlignment="Center" IsReadOnly="True" Margin="5"/>
                                    <TextBox Grid.Column="3" Text="{Binding CreationDate}" HorizontalContentAlignment="Center" IsReadOnly="True" Margin="5"/>
                                    <CheckBox Grid.Column="4" IsChecked="{Binding SelectedForExport}" Margin="5">
                                        <Behaviors:Interaction.Triggers>
                                            <Behaviors:EventTrigger EventName="Checked">
                                                <Behaviors:InvokeCommandAction Command="{Binding Path=DataContext.CTImageSelectionChangedCommand, ElementName=ExportCTSP}" CommandParameter="{Binding}"/>
                                            </Behaviors:EventTrigger>
                                        </Behaviors:Interaction.Triggers>
                                    </CheckBox>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <Button Content="Export CT Data" FontWeight="Bold" Width="170" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,9,0,0" Command="{Binding ExportCTCommand}"></Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
