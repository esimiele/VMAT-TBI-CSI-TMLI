<UserControl x:Class="AutoPlannerHelpers.Views.SpecifyTargetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoPlannerHelpers.Views"
             xmlns:Behaviors="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <Grid>
        <Border Margin="0" BorderThickness="1,0,0,0" BorderBrush="Black">
            <StackPanel HorizontalAlignment="Center" Margin="0">
                <Label Content="Select Targets" FontSize="20" FontStyle="Normal" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                <Grid Height="25" MinWidth ="300" HorizontalAlignment="Stretch" Margin="20,5,20,5">
                    <Button Content="Add Target" Width="100" Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Left" Command="{Binding AddTargetCommand}"/>
                    <Button Content="Add Defaults" Width="120" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Center" Command="{Binding AddDefaultTargetsCommand}"/>
                    <Button Content="Clear List" Width="100" Grid.Column="3"  VerticalAlignment="Top" HorizontalAlignment="Right" Command="{Binding RemoveAllTargetsCommand}"/>
                </Grid>
                <Grid IsSharedSizeScope="True">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="15,0,15,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="col0"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="col1"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="col2"/>
                            <ColumnDefinition Width="*"   SharedSizeGroup="col3"/>
                        </Grid.ColumnDefinitions>
                        <!-- Simple header cell style (optional) -->
                        <Grid.Resources>
                            <Style x:Key="HeaderBorderStyle" TargetType="Border">
                                <Setter Property="Padding" Value="1"/>
                            </Style>
                            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </Grid.Resources>

                        <!-- Column 0: Structure A -->
                        <Border Grid.Column="0" Style="{StaticResource HeaderBorderStyle}">
                            <TextBlock Style="{StaticResource HeaderTextStyle}" Text="Target Id"/>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource HeaderBorderStyle}">
                            <TextBlock Style="{StaticResource HeaderTextStyle}" Text="Rx Dose"/>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource HeaderBorderStyle}">
                            <TextBlock Style="{StaticResource HeaderTextStyle}" Text="Plan Id"/>
                        </Border>

                        <Border Grid.Column="3" Style="{StaticResource HeaderBorderStyle}">
                            <!-- intentionally blank -->
                        </Border>
                    </Grid>
                    <ScrollViewer Grid.Row="1" MaxHeight="390" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <ItemsControl x:Name="TargetsSP" ItemsSource="{Binding Targets}" Margin="15,0,15,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="col0"/>
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="col1"/>
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="col2"/>
                                            <ColumnDefinition Width="*"   SharedSizeGroup="col3"/>
                                        </Grid.ColumnDefinitions>
                                        <ComboBox Grid.Column="0" MinWidth="120" HorizontalContentAlignment="Center" ItemsSource="{Binding Path=DataContext.TargetIds, ElementName=TargetsSP}" SelectedItem="{Binding TargetId}" Margin="1">
                                            <Behaviors:Interaction.Triggers>
                                                <Behaviors:EventTrigger EventName="SelectionChanged">
                                                    <Behaviors:InvokeCommandAction Command="{Binding Path=DataContext.SelectedTargetChangedCommand, ElementName=TargetsSP}" CommandParameter="{Binding}"/>
                                                </Behaviors:EventTrigger>
                                            </Behaviors:Interaction.Triggers>
                                        </ComboBox>
                                        <TextBox Grid.Column="1" MinWidth="90" HorizontalContentAlignment="Center" Text="{Binding TargetRxDose}" Margin="1"/>
                                        <ComboBox Grid.Column="2" MinWidth="120" HorizontalContentAlignment="Center" ItemsSource="{Binding Path=DataContext.PlanIds, ElementName=TargetsSP}" SelectedItem="{Binding PlanId}" Margin="1">
                                            <Behaviors:Interaction.Triggers>
                                                <Behaviors:EventTrigger EventName="SelectionChanged">
                                                    <Behaviors:InvokeCommandAction Command="{Binding Path=DataContext.SelectedPlanChangedCommand, ElementName=TargetsSP}" CommandParameter="{Binding}"/>
                                                </Behaviors:EventTrigger>
                                            </Behaviors:Interaction.Triggers>
                                        </ComboBox>
                                        <Button Content="Clear" MinWidth="50" Grid.Column="3" Command="{Binding Path=DataContext.ClearRowCommand, ElementName=TargetsSP}" CommandParameter="{Binding}" Width="90" Margin="1"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
                
                <Button Content="Set Targets" FontWeight="Bold" Width="150" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="5" Command="{Binding SetTargetsCommand}"></Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
