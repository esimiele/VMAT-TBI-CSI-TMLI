<UserControl x:Class="AutoPlannerHelpers.Views.PlanPreparationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoPlannerHelpers.Views"
             mc:Ignorable="d" Width="570">
    <UserControl.Resources>
        <Style x:Key="TextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="Red" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=Text, RelativeSource={RelativeSource Self}}" Value="YES">
                    <Setter Property="Background" Value="ForestGreen" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border BorderBrush="Black" BorderThickness="1,0,0,0">
            <StackPanel>
                <Label Content="Prepare Plan For Treatment" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Plan:" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" FontWeight="Bold" Margin="5"/>
                    <Label x:Name="planIdLabel" Content="{Binding PlanId}" Grid.Column="1" FontWeight="Bold" HorizontalAlignment="Left" Margin="0" VerticalAlignment="Center"/>
                </Grid>
                
                <Grid Height="40" Width ="400" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
                    <Label Content="Item" FontSize="20" FontStyle="Normal" HorizontalAlignment="Center" Margin="0,0,200,0"></Label>
                    <Label Content="Completed?" FontSize="20" FontStyle="Normal" HorizontalAlignment="Center" Margin="200,0,0,0"></Label>
                </Grid>
                <Grid Height="40" Width ="400" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0">
                    <Button Content="Generate shift note" Width="120" Height="25" HorizontalAlignment="Center" Margin="0,0,200,0" Command="{Binding GenerateShiftNoteCommand}"></Button>
                    <TextBox Text="{Binding GenerateShiftText}" IsReadOnly="True" FontWeight="Bold" TextAlignment="Center" Foreground="black" Style="{StaticResource TextBoxStyle}" Width="70" Height="20" Margin="200,0,0,0"></TextBox>
                </Grid>
                <Grid Height="40" Width ="400" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0">
                    <Button Content="Separate plans" Width="100" Height="25" HorizontalAlignment="Center" Margin="0,0,200,0" Command="{Binding SeparatePlansCommand}"></Button>
                    <TextBox Text="{Binding SeparatePlanText}" IsReadOnly="True" FontWeight="Bold" TextAlignment="Center" Foreground="black" Style="{StaticResource TextBoxStyle}" Width="70" Height="20" Margin="200,0,0,0"></TextBox>
                </Grid>
                <Grid Height="40" Width ="400" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0" Visibility="{Binding DoseCalculationVisibility}">
                    <Button Content="Calculate Dose (optional)" Width="150" Height="25" HorizontalAlignment="Center" Margin="0,0,200,0" Command="{Binding CalculateDoseCommand}"></Button>
                    <TextBox Text="{Binding CalculateDoseText}" IsReadOnly="True" FontWeight="Bold" TextAlignment="Center" Foreground="black" Style="{StaticResource TextBoxStyle}" Width="70" Height="20" Margin="200,0,0,0"></TextBox>
                </Grid>
                <Label Content="More features coming in v16!" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" Margin="0,75,0,0"></Label>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
