<UserControl x:Class="AutoPlannerHelpers.Views.RingGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoPlannerHelpers.Views"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <Grid>
        <Border Margin="0" BorderThickness="1,0,0,0" BorderBrush="Black">
            <StackPanel HorizontalAlignment="Center" Margin="0">
                <Label Content="Create Ring Structures" FontSize="20" FontStyle="Normal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0"></Label>
                <Grid Height="25" Width ="560" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <Button Content="Add Ring" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding AddRingCommand}"/>
                    <Button Content="Add Defaults" Width="100" HorizontalAlignment="Center" VerticalAlignment="Top" Command="{Binding AddDefaultRingsCommand}"/>
                    <Button Content="Clear List" Width="100" HorizontalAlignment="Right" VerticalAlignment="Top" Command="{Binding ClearRingListCommand}"/>
                </Grid>
                <ScrollViewer Height="360" Width="515" Margin="0,5,0,0" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <ItemsControl x:Name="RingStructuresSP" ItemsSource="{Binding RequestedRingStructures}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.2*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0" HorizontalAlignment="Center" HorizontalContentAlignment="Center" ItemsSource="{Binding DataContext.StructureIdsPostUnion, ElementName=RingStructuresSP}" SelectedItem="{Binding TargetId}" MinWidth="120"/>
                                    <TextBox Grid.Column="1" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Text="{Binding MarginFromTargetInCM}" MinWidth="80"/>
                                    <TextBox Grid.Column="2" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Text="{Binding RingThicknessInCM}" MinWidth="80"/>
                                    <TextBox Grid.Column="3" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Text="{Binding DoseLevel}" MinWidth="80"/>
                                    <Button Content="Additional Operation" Grid.Column="4" ToolTip="{Binding AdditionalStructureOperation.FriendlyName}" Command="{Binding Path=DataContext.SpecifyAdditionalOperationCommand, ElementName=RingStructuresSP}" CommandParameter="{Binding}" Width="90" Margin="1"/>
                                    <Button Content="Clear" Grid.Column="5" Command="{Binding Path=DataContext.ClearRowCommand, ElementName=RingStructuresSP}" CommandParameter="{Binding}" Width="90" Margin="1"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
