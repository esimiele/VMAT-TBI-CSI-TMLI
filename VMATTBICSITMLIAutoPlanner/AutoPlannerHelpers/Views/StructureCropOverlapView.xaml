<UserControl x:Class="AutoPlannerHelpers.Views.StructureCropOverlapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoPlannerHelpers.Views"
             mc:Ignorable="d">
    <Grid>
        <Border Margin="0" BorderThickness="1,0,0,0" BorderBrush="Black">
            <StackPanel HorizontalAlignment="Center" Margin="0">
                <Grid Width="580" HorizontalAlignment="Center" Margin="0,0,0,0">
                    <Label Content="Crop/Contour Overlap With Targets" FontSize="20" FontStyle="Normal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0"></Label>
                    <Button Content="?" Width="20" Height="24" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Height="25" Width ="560" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <Button Content="Add OAR" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding AddCropOverlapStructureCommand}"/>
                    <Button Content="Add Defaults" Width="100" HorizontalAlignment="Center" VerticalAlignment="Top" Command="{Binding AddDefaultCropOverlapStructuresCommand}"/>
                    <Button Content="Clear List" Width="100" HorizontalAlignment="Right" VerticalAlignment="Top" Command="{Binding ClearCropOverlapStructureListCommand}"/>
                </Grid>
                <ScrollViewer Height="360" Width="585" Margin="0,5,0,0" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <ItemsControl x:Name="CropOverlapStructuresSP" ItemsSource="{Binding CropOverlapStructures}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0" Width="120" HorizontalContentAlignment="Center" ItemsSource="{Binding Path=DataContext.StructureIdsPostUnion, ElementName=CropOverlapStructuresSP}" SelectedItem="{Binding .}" Margin="1"/>
                                    <Button Content="Clear" Grid.Column="1" Command="{Binding Path=DataContext.RemoveCropOverlapStructureCommand, ElementName=CropOverlapStructuresSP}" CommandParameter="{Binding}" Width="90" Margin="1"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
