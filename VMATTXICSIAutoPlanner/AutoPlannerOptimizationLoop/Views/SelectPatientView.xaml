<Window x:Class="AutoPlannerOptimizationLoop.Views.SelectPatientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoPlannerOptimizationLoop.Views"
        xmlns:enum="clr-namespace:AutoPlannerHelpers.Enums;assembly=AutoPlannerHelpers"
        mc:Ignorable="d"
        Title="SelectPatient" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight" Background="DarkGray">
    <Grid>
        <StackPanel>
            <Grid VerticalAlignment="Top" Background="White" Height="Auto">
                <StackPanel>
                    <Label Content="Manual Entry" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                    <Grid Margin="10,0,10,10" HorizontalAlignment="Center" Width="240">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Patient MRN:" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0"></Label>
                        <TextBox Height="22" Width="120" Text="{Binding MRN}" Grid.Column="1" TextAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0"></TextBox>
                    </Grid>
                    <Grid Margin="5" HorizontalAlignment="Center" Width="240">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Plan Type:" Grid.Column="0" HorizontalAlignment="Right"/>
                        <ComboBox Grid.Column="1" Width="120" HorizontalContentAlignment="Center" SelectedItem="{Binding SelectedPlanType}" ItemsSource="{Binding Source={local:EnumBindingSource {x:Type enum:PlanType}}}" HorizontalAlignment="Left"/>
                    </Grid>
                </StackPanel>
            </Grid>
            <Grid VerticalAlignment="Bottom" Height="Auto">
                <StackPanel>
                    <Label Content="Select Patient From Script Logs" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Label Content="VMAT TBI" Grid.Column="0" Grid.Row="0" FontWeight="Bold" HorizontalAlignment="Center" Margin="0"/>
                        <Label Content="VMAT CSI" Grid.Column="1" Grid.Row="0" FontWeight="Bold" HorizontalAlignment="Center" Margin="0" Visibility="Visible"/>
                        <Label Content="VMAT TMLI" Grid.Column="2" Grid.Row="0" FontWeight="Bold" HorizontalAlignment="Center" Margin="0" Visibility="Visible"/>
                        <ListBox Grid.Column="0" Grid.Row="1" ItemsSource="{Binding PatientMRNsTBI}" SelectedItem="{Binding SelectedLogFile}" HorizontalContentAlignment="Center" Height="300" Width="160" Margin="10,0,10,10"/>
                        <ListBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding PatientMRNsCSI}" SelectedItem="{Binding SelectedLogFile}" HorizontalContentAlignment="Center" Height="300" Width="160" Margin="10,0,10,10" Visibility="Visible"/>
                        <ListBox Grid.Column="2" Grid.Row="1" ItemsSource="{Binding PatientMRNsTMLI}" SelectedItem="{Binding SelectedLogFile}" HorizontalContentAlignment="Center" Height="300" Width="160" Margin="10,0,10,10" Visibility="Visible"/>
                    </Grid>
                    <Grid>
                    </Grid>
                    <Button Content="Open Patient" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="100" Margin="0,0,0,5" Command="{Binding OpenPatientCommand}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
