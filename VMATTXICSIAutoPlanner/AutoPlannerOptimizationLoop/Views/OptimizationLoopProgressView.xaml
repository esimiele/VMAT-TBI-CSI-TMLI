<Window x:Class="AutoPlannerOptimizationLoop.Views.OptimizationLoopProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoPlannerOptimizationLoop.Views"
        xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
        mc:Ignorable="d"
        HorizontalAlignment="Center" SizeToContent="WidthAndHeight" Background="DarkGray">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding WindowClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <StackPanel>
            <Label Content="Progress of Optimization Loop" FontSize="20" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="5"></Label>
            <ScrollViewer x:Name ="scroller" VerticalAlignment="Bottom" HorizontalAlignment="Center" Width="795" Height="490" Margin="5" BorderBrush="Black" IsTabStop="False" local:ScrollViewerAttachedProperties.ScrollToBottomOnChange="{Binding ProgressInfo}">
                <TextBox Text="{Binding ProgressInfo}" FontSize="12" FontFamily="Courier New" Width="775" Background="White" Padding="3" TextWrapping="Wrap"/>
            </ScrollViewer>
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="290"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding TaskId}" Grid.Column="0" Grid.Row="0" FontWeight="Bold" Width="250" TextAlignment="Center" Height="18" HorizontalAlignment="Right" Margin="0"/>
                <Grid Grid.Column="1" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <ProgressBar Height="20" Margin="0,0,0,0" Width="220" Minimum="0" Maximum="100" Foreground="LimeGreen" Value="{Binding TaskProgress}"/>
                    <TextBlock Text="{Binding TaskProgress, StringFormat={}{0:0}%}" Background="{Binding TaskProgressBackground}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" />
                </Grid>
                <Grid Grid.Column="2" Grid.Row="0" >
                    <Label Content="Overall Progress: " HorizontalAlignment="Left" Margin="2,0,0,0"></Label>
                    <ProgressBar HorizontalAlignment="Right" Height="20" Margin="0,0,0,0" Width="190" Minimum="0" Maximum="100" Foreground="LimeGreen" Value="{Binding OverallProgress}"/>
                    <TextBlock Text="{Binding OverallProgress, StringFormat={}{0:0}%}" Background="{Binding OverallProgressBackground}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,85,0" />
                </Grid>

                <Grid Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" Margin="5">
                    <Button Content="Abort" HorizontalAlignment="Left" Height="20" Width="105" Margin="0" Command="{Binding AbortRunCommand}"/>
                    <TextBox Text="{Binding RunStatus}" Background="{Binding RunStatusBackground, FallbackValue=White}" TextAlignment="Center" HorizontalAlignment="Right" Height="20" Width="125" Margin="0"></TextBox>
                </Grid>
                <Grid Grid.Column="2" Grid.Row="1">
                    <Label Content="Run time:" HorizontalAlignment="Left" FontSize="12" Margin="40,0,0,0"/>
                    <TextBlock Text="{Binding RunTime, FallbackValue=00:00:00}" FontSize="16" Width="190" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0" Foreground="Black"></TextBlock>
                </Grid>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
