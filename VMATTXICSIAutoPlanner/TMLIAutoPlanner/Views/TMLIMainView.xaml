<Window x:Class="TMLIAutoPlanner.Views.TMLIMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TMLIAutoPlanner.Views"
        xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
        mc:Ignorable="d"
        Title="VMAT TMLI Autoplanning" 
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding WindowClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="LightGray"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- ========== SUBTABS: Top header, equal widths ========== -->
        <Style x:Key="TopEqualHeaderTabControlStyle" TargetType="TabControl">
            <Setter Property="TabStripPlacement" Value="Top"/>
            <Setter Property="Background" Value="DarkGray"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <!-- headers -->
                                <RowDefinition Height="*"/>
                                <!-- content -->
                            </Grid.RowDefinitions>

                            <!-- Equal-width header strip -->
                            <Border Grid.Row="0" Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="0,0,0,1">
                                <UniformGrid Rows="1" IsItemsHost="True"/>
                            </Border>

                            <!-- Selected tab content -->
                            <Border Grid.Row="1" Background="{TemplateBinding Background}">
                                <ContentPresenter ContentSource="SelectedContent" Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <!-- Subtab header look -->
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="TabItem">
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="MinWidth" Value="120"/>
                        <Setter Property="Background" Value="LightGray"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TabItem}">
                                    <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="Black"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="2,2,0,0"
                                        Padding="6,3">
                                        <ContentPresenter x:Name="ContentSite"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== MAIN TABS: Left header, equal heights ========== -->
        <Style x:Key="LeftEqualHeaderTabControlStyle" TargetType="TabControl">
            <Setter Property="TabStripPlacement" Value="Left"/>
            <Setter Property="Background" Value="DarkGray"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <!-- Optional: how wide your left tab strip should be -->
            <Setter Property="Tag" Value="180"/>
            <!-- treat Tag as 'TabStripWidth' -->

            <!-- Main tab header look (vertical list, no rotation) -->
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="TabItem">
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="MinHeight" Value="24"/>
                        <Setter Property="Background" Value="LightGray"/>
                        <!-- equal height baseline -->
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TabItem}">
                                    <Border x:Name="Border"
                                        BorderBrush="Black"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="2,0,0,2"
                                        Padding="10,6"
                                        Background="{TemplateBinding Background}"
                                        SnapsToDevicePixels="True">
                                        <ContentPresenter x:Name="ContentSite"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                                        </Trigger>

                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Width="auto">
        <StackPanel>
            <Grid>
                <Label Content="VMAT TMLI Auto Planning" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Top"/>
                <Button Content="Help" HorizontalAlignment="Right" Margin="0,10,30,0" VerticalAlignment="Top" Width="75" Command="{Binding HelpGuideCommand}"/>
                <Button Content="Quick Start" HorizontalAlignment="Left" Margin="30,10,0,0" VerticalAlignment="Top" Width="75" Command="{Binding QuickStartGuideCommand}"/>
            </Grid>
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.ColumnSpan="2" Grid.Row="0" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Label Content="Patient MRN:" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Right" Margin="0" VerticalAlignment="Center"/>
                    <Label Content="{Binding PatientMRN}" Grid.Column="1" Grid.Row="0" FontWeight="Bold" FontSize="12" HorizontalAlignment="Center" HorizontalContentAlignment="Center" Width="140"/>

                    <Label Content="Structure Set ID:" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    <Label Content="{Binding StructureSetId}" Grid.Column="1" Grid.Row="1" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Width="140"/>
                </Grid>

                <Grid Grid.Column="0" Grid.Row="1" Grid.RowSpan="4" Margin="5,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="3*"/>
                    </Grid.RowDefinitions>
                    <Label Content="Plan Templates" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,10,0"></Label>
                    <ListBox ItemsSource="{Binding PlanTemplates}" SelectedItem="{Binding SelectedTemplate}" Grid.Row="1" Width="320" HorizontalContentAlignment="Center" DisplayMemberPath="TemplateName"/>
                </Grid>

                <Grid Grid.Column="1" Grid.Row="1" Grid.RowSpan="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.8*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="TMLI Plan Rx" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"></Label>

                    <Label Content="Dose per fraction (cGy/fx):" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center"></Label>
                    <Label Content="Number of fractions:" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center"></Label>
                    <Label Content="Rx dose (cGy):" Grid.Row="3" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center"></Label>

                    <TextBox Text="{Binding InitialDosePerFraction}"  Grid.Row="1" Grid.Column="1" TextAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center" Width="75"></TextBox>
                    <TextBox Text="{Binding InitialNumberOfFractions}" Grid.Row="2" Grid.Column="1" TextAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center" Width="75"></TextBox>
                    <TextBox Text="{Binding InitialPlanTotalDose}" Grid.Row="3" Grid.Column="1" TextAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center" Width="75" IsReadOnly="True"></TextBox>
                </Grid>
            </Grid>

            <Grid Background="DarkGray">
                <TabControl SelectedIndex="{Binding InitialTabSelected}" Style="{StaticResource LeftEqualHeaderTabControlStyle}">
                    <TabItem Header="Stitch CT" Content="{Binding StitchCT}" Background="{Binding StitchCTTabBackground}" BorderThickness="0,1,1,0" Visibility="{Binding StitchCTTabVisible, FallbackValue=Visible}">
                    </TabItem>
                    <TabItem Header="Export/Import" BorderThickness="0,1,1,0">
                        <TabControl Style="{StaticResource TopEqualHeaderTabControlStyle}">
                            <TabItem Header="Export CT" Content="{Binding ExportCT}" BorderThickness="1,1,1,1">
                            </TabItem>
                            <TabItem Header="Import Structure Set" Content="{Binding ImportSS}" BorderThickness="1,1,1,1"/>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="Specify Targets" Background="{Binding SpecifyTargetsTabBackground}" BorderThickness="0,1,1,0">
                        <TabControl Style="{StaticResource TopEqualHeaderTabControlStyle}">
                            <TabItem Content="{Binding TargetStructureDerivations}" Background="{Binding TargetStructureDerivationsBackground}" Header="Derive Targets" BorderThickness="1,1,1,1">
                            </TabItem>
                            <TabItem Header="Set Targets" BorderThickness="0,1,1,1" Content="{Binding SpecifyTargets}" Background="{Binding SetTargetsTabBackground}">
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="Structure Tuning" Background="{Binding StructureTuningTabBackground}" BorderThickness="0,1,1,0" Margin="0,0,1,1">
                        <TabControl Style="{StaticResource TopEqualHeaderTabControlStyle}">
                            <TabItem Header="Special Opt Structures" Content="{Binding SpecialOptimizationStructures}" BorderThickness="1,1,1,1" >
                            </TabItem>
                            <TabItem Header="Ring Generation" Content="{Binding RingGeneration}" BorderThickness="0,1,1,1" >
                            </TabItem>
                            <TabItem Header="Structure Derivations" Content="{Binding OptimizationStructureDerivations}" Background="{Binding OptimizationStructureDerivationBackground}" BorderThickness="0,1,1,1">
                            </TabItem>
                        </TabControl>
                    </TabItem>
                    <TabItem Header="Beam Placement" Content="{Binding BeamPlacement}" Background="{Binding BeamPlacementTabBackground}" BorderThickness="0,1,1,0">
                    </TabItem>
                    <TabItem Header="Optimization Setup" Content="{Binding OptimizationSetup}" Background="{Binding OptimizationSetupTabBackground}" BorderThickness="0,1,1,0">
                    </TabItem>
                    <TabItem Header="Plan Preparation" Content="{Binding PlanPreparation}" BorderThickness="0,1,1,1">
                    </TabItem>
                    <TabItem Header="Script Configuration" Content="{Binding ScriptConfiguration}" HorizontalContentAlignment="Center" VerticalAlignment="Bottom" BorderThickness="0,1,1,1">
                    </TabItem>
                </TabControl>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
