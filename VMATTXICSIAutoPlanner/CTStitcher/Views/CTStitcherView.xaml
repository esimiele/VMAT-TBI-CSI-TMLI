<UserControl x:Class="CTStitcher.Views.CTStitcherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CTStitcher.Views"
             mc:Ignorable="d" 
             Background="DarkGray">
    <Viewbox Stretch="None">
        <Grid Background="DarkGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Row="1" Grid.Column="1" >
                <Grid HorizontalAlignment="Center" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="230"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Head First Supine Scan:" Grid.Column="0" HorizontalAlignment="Right" FontSize="18" Margin="5"/>
                    <ComboBox ItemsSource="{Binding HFSCTScans}" SelectedItem="{Binding SelectedHFSScan}" Grid.Column="1" HorizontalContentAlignment="Center" HorizontalAlignment="Center" VerticalContentAlignment="Center" Width="200" Margin="5"/>
                </Grid>
                <Grid HorizontalAlignment="Center" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="230"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Feet First Supine Scan:" Grid.Column="0" HorizontalAlignment="Right" FontSize="18" Margin="5"/>
                    <ComboBox ItemsSource="{Binding FFSCTScans}" SelectedItem="{Binding SelectedFFSScan}"  Grid.Column="1" HorizontalContentAlignment="Center" HorizontalAlignment="Center" VerticalContentAlignment="Center" Width="200" Margin="5"/>
                </Grid>
                <Grid HorizontalAlignment="Center" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="230"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Stitching Algorithm:" Grid.Column="0" HorizontalAlignment="Right" FontSize="18" Margin="5"/>
                    <ComboBox ItemsSource="{Binding Algorithms}" SelectedItem="{Binding SelectedAlgorithm}"  Grid.Column="1" HorizontalContentAlignment="Center" HorizontalAlignment="Center" VerticalContentAlignment="Center" Width="200" Margin="5"/>
                </Grid>
                <Grid HorizontalAlignment="Center" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="230"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Review Progress:" Grid.Column="0" HorizontalAlignment="Right" FontSize="18" Margin="5"/>
                    <Grid Width="200" Grid.Column="1">
                        <ProgressBar BorderThickness="1" BorderBrush="Black" HorizontalAlignment="Center" Height="30" Width="200" Minimum="0" Maximum="100" Foreground="ForestGreen" Value="{Binding ReviewProgress}"/>
                        <TextBlock Text="{Binding ReviewProgress, StringFormat={}{0:0}%}" HorizontalAlignment="Center" FontSize="18" VerticalAlignment="Center" Margin="0,0,5,0" />
                    </Grid>
                </Grid>
                <Button Command="{Binding PushStitchedCTToAriaCommand}" Content="Approve image and push CT to Aria" Width="300" FontSize="16" FontWeight="Bold" Height="30" Grid.Column="1" Margin="20" Visibility="{Binding PushToAriaVisibility}"/>
            </StackPanel>

            <Grid x:Name="axialGrid" Grid.Column="0" Grid.Row="1" Width="500" Height="330" Background="Black" MouseWheel="MouseWheelMove" Margin="5">
                <local:ZoomBorder x:Name="axialBorder" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
                    <Image x:Name="axialImage" Source="{Binding DisplayedAxialImage}"/>
                </local:ZoomBorder>
            </Grid>
            <Grid x:Name="coronalGrid"  Grid.Column="0" Grid.Row="2" Width="500" Height="330" Background="Black" MouseWheel="MouseWheelMove" Margin="5">
                <local:ZoomBorder x:Name="coronalBorder" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
                    <Image x:Name="coronalImage" Source="{Binding DisplayedCoronalImage}"/>
                </local:ZoomBorder>
            </Grid>

            <Grid x:Name="sagittalGrid"  Grid.Column="1" Grid.Row="2" Width="500" Height="330" Background="Black" MouseWheel="MouseWheelMove" Margin="5">
                <local:ZoomBorder x:Name="sagittalBorder" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
                    <Image x:Name="sagittalImage" Source="{Binding DisplayedSagittalImage}"/>
                </local:ZoomBorder>
            </Grid>

            <Grid Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="400"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="410"/>
                </Grid.ColumnDefinitions>
                <Button Content="Select DCM CT Folder" Command="{Binding ReadCTFromDICOMCommand}" Grid.Column="0" Grid.Row="3" Width="140" Margin="5"/>

                <Label Grid.Column="1" Content="Axial Slice:" HorizontalAlignment="Right"/>
                <TextBox Text="{Binding AxialSliceNumber}" HorizontalContentAlignment="Center" Grid.Column="2" Background="White" Height="20" Width="50" HorizontalAlignment="left"></TextBox>

                <Grid Grid.Column="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <Button Command="{Binding StitchCTsCommand}" Content="Stitch CTs Together" Width="140" Grid.Column="0" Grid.Row="3" Margin="5"/>
                </Grid>
            </Grid>
            <Expander HorizontalAlignment="Right" HorizontalContentAlignment="Center" VerticalAlignment="Bottom" Header="Log Output" Margin="5,5,5,5" Grid.RowSpan="3" Grid.Row="1" Grid.Column="2">
                <Expander.LayoutTransform>
                    <RotateTransform Angle="-90"></RotateTransform>
                </Expander.LayoutTransform>
                <Grid>
                    <StackPanel>
                        <StackPanel.LayoutTransform>
                            <RotateTransform Angle="90"></RotateTransform>
                        </StackPanel.LayoutTransform>
                        <ScrollViewer x:Name = "scroller" Width="500" Height="660" HorizontalAlignment="Right" Margin="5,5,5,5">
                            <TextBlock Text="{Binding UILogOutput}" Background="White" Padding="2" TextWrapping="Wrap"/>
                        </ScrollViewer >
                        <Button Content="Push CT to Aria/Autocontouring" Command="{Binding BypassStitchingAndPushToAriaCommand}" Width="190" Margin="5,5,5,0"/>
                    </StackPanel>

                </Grid>
            </Expander>
        </Grid>
    </Viewbox>
</UserControl>
